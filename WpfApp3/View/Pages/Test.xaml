<Page x:Class="WpfApp3.View.Pages.Test"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp3.View.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Test">

    <Grid>
        <Border Height="150" Width="800" BorderThickness="1" BorderBrush='Black' Background="White">
            <Grid>
                <StackPanel VerticalAlignment="Center"
                            HorizontalAlignment="Left">
                    <TextBlock Text="Название: " FontSize="14">
                        <Run Text="{Binding title}"/>
                    </TextBlock>
                    <TextBlock Text="Цена: " FontSize="14">
                        <Run Text="{Binding price}"/>
                    </TextBlock>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Page>
